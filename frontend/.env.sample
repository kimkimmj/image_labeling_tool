# 현재 빌드는 기본값으로 `/api` 상대 경로 + Vite dev 프록시를 사용합니다.
# 별도 `VITE_*` 변수 없이도 docker-compose.prod.yml 의 nginx가 `/api` 를 백엔드로 넘깁니다.
#
# 나중에 API를 다른 호스트에 두고 절대 URL로 부를 경우에만 아래 예시를 사용하세요.
#
# VITE_API_BASE_URL=https://api.example.com
